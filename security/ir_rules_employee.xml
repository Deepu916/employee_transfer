<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data noupdate="1">
        <record id="employee_rule" model="ir.rule">
            <field name="name">Employee rule User</field>
            <field name="model_id" ref="employee_transfer.model_employee_transfer"/>
            <field name="groups" eval="[(4,ref('employee_transfer.employee_group'))]"/>
            <field name="domain_force">['|',('state','!=','draft'),('create_uid','=',user.id),('company_id','=',user.env.company.id)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <record id="employee_multi_company_rule_admin" model="ir.rule">
            <field name="name">Multi Company Rule</field>
            <field name="model_id" ref="base.model_res_company"/>
            <field name="groups" eval="[(4,ref('base.group_system'))]"/>
        </record>

        <record id="employee_rule_admin" model="ir.rule">
            <field name="name">Employee rule Admin</field>
            <field name="model_id" ref="employee_transfer.model_employee_transfer"/>
            <field name="groups" eval="[(4,ref('base.group_system'))]"/>
            <field name="domain_force">['|',('state','!=','draft'),('create_uid','=',user.id),('new_company_id','=',user.env.company.id)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

    </data>
</odoo>